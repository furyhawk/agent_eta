
# Deployment for agent_eta Backend
apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent_eta-backend
  namespace: agent_eta
  labels:
    app.kubernetes.io/name: agent_eta
    app.kubernetes.io/component: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: agent_eta
      app.kubernetes.io/component: backend
  template:
    metadata:
      labels:
        app.kubernetes.io/name: agent_eta
        app.kubernetes.io/component: backend
    spec:
      containers:
        - name: backend
          image: agent_eta:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8500
              name: http
          envFrom:
            - configMapRef:
                name: agent_eta-config
            - secretRef:
                name: agent_eta-secrets
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /api/v1/health
              port: 8500
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /api/v1/ready
              port: 8500
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          volumeMounts:
            - name: media-data
              mountPath: /app/media
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
      volumes:
        - name: media-data
          persistentVolumeClaim:
            claimName: agent_eta-media
      restartPolicy: Always

---
# Celery Worker Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent_eta-celery-worker
  namespace: agent_eta
  labels:
    app.kubernetes.io/name: agent_eta
    app.kubernetes.io/component: celery-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: agent_eta
      app.kubernetes.io/component: celery-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: agent_eta
        app.kubernetes.io/component: celery-worker
    spec:
      containers:
        - name: celery-worker
          image: agent_eta:latest
          imagePullPolicy: Always
          command: ["celery", "-A", "app.worker.celery_app", "worker", "--loglevel=info"]
          envFrom:
            - configMapRef:
                name: agent_eta-config
            - secretRef:
                name: agent_eta-secrets
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      restartPolicy: Always

---
# Celery Beat Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent_eta-celery-beat
  namespace: agent_eta
  labels:
    app.kubernetes.io/name: agent_eta
    app.kubernetes.io/component: celery-beat
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: agent_eta
      app.kubernetes.io/component: celery-beat
  template:
    metadata:
      labels:
        app.kubernetes.io/name: agent_eta
        app.kubernetes.io/component: celery-beat
    spec:
      containers:
        - name: celery-beat
          image: agent_eta:latest
          imagePullPolicy: Always
          command: ["celery", "-A", "app.worker.celery_app", "beat", "--loglevel=info"]
          envFrom:
            - configMapRef:
                name: agent_eta-config
            - secretRef:
                name: agent_eta-secrets
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "200m"
      restartPolicy: Always
